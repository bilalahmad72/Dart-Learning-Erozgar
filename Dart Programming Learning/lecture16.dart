// return:
// - return is a keyword
// - a function can return only one thing
// - a function will run return keyword only once
// - the function will exit at return keyword
// - The lines after 'return' keyword will never execute

// return type (optional)
// - we can optionally specify return type of a funciton
// - A function can have any return type ( int, String, double, bool, List, Set, Map, HashMap, Object)
// - Return type will depend on the data-type of returned value
void main() {
// function calling
// ****************
abc(["Amar","Wahid", "Kashif"],{01,02,03}, {"name":"Shahzaib","roll_no":05, "gpa":3.9} );
}

void abc(List<String> names, Set<int> userIds, Map<String, dynamic> topperDetails) {

}



// Parameters/params/Arguments/function-input/variable:
// - We can call Params as varriables
// - syntax wise, params are 90% similar of variables
// - We specify params inside function's parenthesis
// - we can have unlimited params in a function
// - A function can have different types of params
// - Params are accessible only inside funciton body




// function signature
String userData(String name, int rollNo, String gpa) {

return "";
}